By default, all endpoints are secured.
SS configures http basic and form login methods
A random password is generated by SS everytime you run the application.

If you don't provide credentials, you will get 401 status code
when you enter the credentials, they are encoded into base64 format.
